numpy==1.26.0
scipy==1.12.0
matplotlib==3.8.0
nbconvert
ipykernel

# Required only when using parallel_processing = 'mpi'.
# Install this against the MPI implementation available on your cluster.
mpi4py
